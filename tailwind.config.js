module.exports = {
    theme: {
        extend: {
            colors: {
                base: "var(--bg)",
                baseStrong: "var(--bg-strong)",
                textColor: "var(--text)",
                borderColor: "var(--border)"
            }
        }
    }
}
